#include "party.h"
/*
int inicializar_personajes(t_party* party)
{
    unsigned int result;
    for(unsigned int i = 0; i < CANTIDAD_DE_PERSONAJES; i++) {
        result = inicializar_personaje(&party->personajes_arr[i], i);
        party->flags_de_personajes
        if(result != 0)
            return 1;
    }
    return 0;
}
*/
